# YooAsset 文档发布检查清单

本文档用于检查 `yoo-website` 目录下的 YooAsset 网站文档，重点面向日常发布前的质量复查。检查目标是确保文档内容准确、链接有效、资源完整、页面展示正常。

## 1. 发布范围确认

- 确认本次修改涉及的版本目录、文档目录和页面。
- 检查新增、删除、重命名页面是否符合当前文件命名和编号约定。
- 检查入口页、索引页、侧栏和正文交叉引用是否同步更新。
- 检查是否存在空页面、重复主题页面或无人引用的孤立页面。
- 检查不应修改的目录是否保持不变，例如需要冻结的历史版本或 API 文档目录。

## 2. 链接与资源

- 检查文档内部相对链接是否有效，例如：
  - `./xxx.md`
  - `../xxx.md`
  - `./Image/xxx.png`
- 检查图片、附件和示例资源是否真实存在，路径大小写是否一致。
- 检查删除或重命名页面后，是否还有正文、侧栏、图片或外部入口引用残留。
- 检查图片文件是否存在孤立资源，尤其是被删除页面专属图片。
- 检查 GitHub 链接是否有效，并确认源码链接指向当前文档版本对应的维护分支。
- 检查外部链接是否仍可访问，尤其是 Unity、小游戏平台、OpenUPM、教程和 issue 链接。

## 3. 内容一致性

- 检查术语、产品名、平台名、功能名是否统一，例如：
  - `YooAsset`
  - `WebGL`
  - `Builtin`
  - `ResourcePackage`
  - `AssetBundle`
- 检查同一功能在不同页面中的描述是否冲突。
- 检查历史说明是否明确表达为历史信息，避免读者误解为当前行为。
- 检查入口页、教程、FAQ、解决方案、小游戏文档之间是否互相对应。
- 检查功能描述是否和当前源码、示例工程、菜单名称、窗口名称保持一致。

## 4. 代码示例

- 检查示例变量名是否前后一致，例如 `package`、`operation`、`handle`。
- 检查示例是否存在明显拼写错误或语法错误，例如：
  - `yield reurn`
  - 错误的类型名
  - 错误的枚举值
  - 不存在的方法或属性
- 检查示例流程是否完整，例如初始化、请求版本、加载清单、下载、加载、释放等步骤是否符合上下文。
- 检查代码块中省略代码的位置是否不会造成变量未声明、重复调用或流程歧义。
- 检查平台相关示例是否符合平台限制，例如 WebGL 和小游戏平台的同步加载、原生文件、下载能力限制。
- 检查代码示例所引用的示例工程路径、类名和文件名是否仍然存在。

## 5. 文档结构

- 检查版本目录命名和排序是否符合当前约定。
- 检查页面文件编号是否自然，避免重复编号或明显跳跃造成排序混乱。
- 检查章节标题层级是否合理，不要跳级过多。
- 检查章节标题、页面标题和侧栏标题是否表达一致。
- 检查 FAQ、解决方案、小游戏、迁移指南、CDN 等入口页是否命名清晰，排序自然。

## 6. 页面展示

- 检查 Markdown 表格在网页中是否正常铺开，长内容是否出现不必要的挤压。
- 检查代码块语言标记是否正确，例如 `csharp`、`json`、`shell`、`mdx`。
- 检查图片是否清晰、比例正常、位置正确。
- 检查页面中的列表、引用、删除线、加粗、链接文本是否显示正常。
- 检查浏览器中的侧栏、正文、右侧目录、锚点跳转和移动端横向滚动是否正常。
- 检查页面是否存在明显的中英文空格、标点、大小写或排版不一致问题。

## 7. 推荐命令

在 `yoo-website` 目录下执行：

```shell
git diff --check
```

检查空白问题。

检查 YooAsset GitHub 链接是否仍指向不合适的分支：

```shell
rg "github\.com/tuyoogame/YooAsset/(blob|tree)/(dev|main|master)" versioned_docs
```

检查临时标记是否残留：

```shell
rg "TODO|FIXME|待补充|临时|测试用" versioned_docs docs
```

检查图片引用：

```shell
rg "\./Image/|\.\./.*/Image/" versioned_docs docs
```

## 8. 建议检查顺序

1. 确认本次文档变更范围。
2. 检查页面结构、文件编号和侧栏入口。
3. 检查内部链接、外部链接和图片资源。
4. 检查代码示例、术语命名和跨页面描述一致性。
5. 检查重点页面的表格、代码块、图片、侧栏和跳转展示。
6. 运行基础文本检查命令。

## 附录：版本升级专项检查

当文档从旧版本复制、迁移或升级到新版本时，再额外执行本节检查。

- 检查是否还有旧版本 API 示例残留，例如：
  - `InitializeAsync`
  - `EOperationStatus.Succeed`
  - `FileSystemParametersDefine`
  - `BeginDownload`
  - `ClearCacheFilesAsync`
  - `UpdatePackageManifestAsync`
  - `PreDownloadContentAsync`
- 检查新版本名称是否统一，例如：
  - `Builtin` 不应误写为 `Buildin`
  - 构建管线名称、参数名称应与源码一致
  - 枚举参数名称不应继续写成旧常量名
- 检查已移除功能是否仍被描述为当前功能。
- 检查迁移指南、兼容层文档和源码中的 `[Obsolete]` 说明是否一致。
- 检查历史版本说明是否明确标注为历史信息，避免读者误解为当前版本行为。

